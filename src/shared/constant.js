const RESULT_MATCH = ["PENDING", "WIN", "LOSE"];
const STATE_MATCH = ["INPROGRESS", "FINISHED"];
const STATUS_MATCH = ["active", "deactivate"];
const TEAM_OF_MATCH = ["T", "CT"];

module.exports = {
  RESULT_MATCH,
  STATE_MATCH,
  STATUS_MATCH,
  TEAM_OF_MATCH,
};
